syntax = "proto3";

package social.mixi.application.const.v1;

// メッセージに添付されるメディア種別を示す列挙型
enum MediaType {
  // 未指定
  MEDIA_TYPE_UNSPECIFIED = 0;
  // 画像
  MEDIA_TYPE_IMAGE = 1;
  // 動画
  MEDIA_TYPE_VIDEO = 2;
}
