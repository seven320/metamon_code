syntax = "proto3";

package social.mixi.application.const.v1;

// ポストに添付されるメディア種別を示す列挙型
enum PostMediaType {
  // 未指定
  POST_MEDIA_TYPE_UNSPECIFIED = 0;
  // 画像
  POST_MEDIA_TYPE_IMAGE = 1;
  // 動画
  POST_MEDIA_TYPE_VIDEO = 2;
}
