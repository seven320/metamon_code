syntax = "proto3";

package social.mixi.application.const.v1;

// ユーザーの公開設定を示す列挙型
enum UserAccessLevel {
  // 未指定
  USER_ACCESS_LEVEL_UNSPECIFIED = 0;
  // 公開ユーザー
  USER_ACCESS_LEVEL_PUBLIC = 1;
  // 非公開ユーザー
  USER_ACCESS_LEVEL_PRIVATE = 2;
}
