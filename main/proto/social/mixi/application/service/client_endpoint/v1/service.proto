syntax = "proto3";

package social.mixi.application.service.client_endpoint.v1;

import "social/mixi/application/model/v1/event.proto";

// Webhook によるイベント送信リクエストです。
message SendEventRequest {
  // 送信するイベントの一覧です。
  repeated model.v1.Event events = 1;
}
